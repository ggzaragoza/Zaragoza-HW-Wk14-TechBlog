{{!-- <section class="container">
  <h3>{{post.title}}</h3>
  <h4>by {{post.username}}</h4>
  <section class="flex-row justify-center">
    <p>{{post.content}}</p>
  </section>
  <section class="flex-row justify-center">
    <p>{{comment.content}}</p>
  </section>

<div class="form-group">
  <label for="add-comment">Leave a Comment:</label>
  <input class="form-input" type="comment" id="new-comment" />
</div>

</section> --}}

<div class="text-left">
  <h2>{{post.title}}</h2>
  {{!-- <p>{{{get_emoji}}}</p> --}}
  <p>{{post.content}}</p>
  <p>Created by {{post.user.username}} on {{format_date post.date_created}}</p>
  {{!-- <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p> --}}

  <h3>Comments:</h3>
  {{#each comments as |comment|}}
  <div class="row mb-2">
    <div class="col-md-8">
      <p>{{this.content}}<br>
      Posted by {{user.username}}</p>
    </div>
  </div>
  {{/each}}
</div>

{{#if logged_in}}
<h3>Add a Comment:</h3>
<form class="form new-comment-form">
  <div class="form-group">
    <label for="new-comment">New Comment:</label>
    <input class="form-input" type="text" id="new-comment" name="new-comment" />
  </div>
  <div class="form-group">
    <button type="submit" class="btn btn-primary">Publish</button>
  </div>
</form>
{{/if}}

<script src="./js/post.js"></script>







{{!-- post details handlebars --}}

{{!-- <div class="row align-center mb-5">
  <div class="col-md-6">
    <a href="/post/{{id}}"><h2>{{title}} by {{user.username}}</h2></a>
  </div>
  <div class="col-md-6">
    <p>{{content}}</p>
  </div>
</div> --}}