{{!-- {{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <a href="/post/{{post.id}}"><h2>{{post.title}} by {{post.user_id}}</h2></a>
  </div>
  <div class="col-md-6">
    <p>{{post.content}}</p>
  </div>
</div>
{{/each}} --}}

{{!-- {{#each posts as |post| }}
{{> post-details}}
{{/each}} --}}

{{#each posts as |post| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{!-- {{{get_emoji}}} --}}
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </h2>
    {{!-- <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p> --}}
    <p>Created by {{post.user.username}} on {{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.content}}
    </p>
  </div>
</div>
{{/each}}
