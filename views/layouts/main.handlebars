<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>A-Da Blog</title>
  {{!-- <link rel="stylesheet" href="/css/jass.css"> --}}
</head>

<body>
  <header class="display-flex justify-space-between align-center p-2">
    <h1>
      A-Da Blog
    </h1>
    <!-- Only display the logout button if the user is logged in -->
  </header>
  <nav>
    <a href="/">Home</a>
    <button class="btn" id="dash-btn">Dashboard</button>
    {{#if logged_in}}
    <button class="btn" id="logout">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
  </nav>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      {{!-- <h3>twees it out why dont cha</h3> --}}
  </footer>

  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{!-- {{else}}
  <script src="/js/login.js"></script> --}}
  {{/if}}
</body>

</html>
